<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Rooms

    <a data-toggle="collapse" href="#team_rooms_new_form">
      Add room
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @team.rooms.each do |room| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to room, room_path(room) %>

        <%= link_to room, method: :delete, data: { confirm: "Do you really want to delete this room" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="team_rooms_new_form">
        <%= render partial: "teams/team_rooms_new_form" %>
      </div>
    </div>

  </ul>
</div>
